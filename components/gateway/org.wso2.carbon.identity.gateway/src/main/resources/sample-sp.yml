serviceProvider:
  name: "SampleSP"

  handler:
    name: "CustomRequestValidator"
    id: "validator1"
    config:
      url: "http://test"
      ddd:
        fdfd: "Ddd"

    nextHandler:
      name: "RequestPathAuthenticator"
      id: "requestPathAuthenticator"
      config:
        test: "xxx"

      nextHandler:
        name: "CustomResponseBuilder"
        id: "responseBuilder1"
        config:
          test: "xxx"
          abss: "ddd"

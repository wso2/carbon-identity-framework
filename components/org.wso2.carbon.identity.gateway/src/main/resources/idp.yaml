identityProviderConfig:
  name: "googleidp"

  idpMetaData :
    certificates :
      -
        key : ""
        value : ""
    federationHub : "false"
    homeRealm : "dfdf"

    roleConfiguration :
      roleMappings :
        -
          localRoleName : ""
          idpRoleName : ""

  authenticationConfig :
    requestedClaimUris :
      - "http://localhst/identitt/etc"
    authenticatorConfigs:
      -
        name : "google"
        properties :
          xx : "dfdf"

  provisioningConfig :

    jitProvisioningConfig :
      provisioningIdPs :
        - "fdfd"
        - "dfdf"
    provisioningClaimConfigs :
      -
        claimId : ""
        default : ""

    provisioningRoles :
      - "dfdf"
      - "dfd"

    provisionerConfigs :
      -
        name : "dfdf"
        properties :
          fdfd : "sdsd"

